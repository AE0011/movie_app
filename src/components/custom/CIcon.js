import React from 'react';
import IconIo from 'react-native-vector-icons/Ionicons';

const CIcon = ({name, size, color, style}) => {
  return <IconIo name={name} size={size} color={color} style={style} />;
};

export default CIcon;
